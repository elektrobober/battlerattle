# Worklog

- 2026-08-20: подготовил август (config, party.json по листам Антернера и Олкюра), прогнал транскрипцию и AI-анализ 28 чанков; нашёл причину массовых 429 — запрос на ~50к токенов не влезает в лимит tier-1 в 30к TPM, ретраи тут бессильны; починил бэкофф (раздельные бюджеты для 429 и 5xx, чтение reset-заголовков, игнор подсказок короче секунды), добавил провайдер OpenAI Batch с авто-нарезкой пачек по enqueued-лимиту и перевёл на батч синтез сессии; дальше — сгенерировать сцены по image_prompts.md и пересобрать PDF
- 2026-08-26: аудит транскрипции (docs/asr_audit_2026-08.md) — нашёл корень галлюцинаций: на MLX-пути нет VAD, Whisper декодирует все 4ч35м сплошняком; подтвердил по коду mlx_whisper, что initial_prompt живёт только первое 30-секундное окно; разобрал рынок ASR на август 2026 (large-v3 vs turbo, GigaAM v3, Qwen3-ASR, облако) и отдельно диаризацию — вывод: на однодорожечный звук не переходить (DER 33-48% на TTRPG-аудио по arXiv 2502.12714); дальше — VAD-нарезка и cross-track gating
- 2026-08-26 (2): реализовал VAD на MLX-пути (Silero из faster-whisper → clip_timestamps, бакеты ~10 мин с переинжектом initial_prompt), word_timestamps=True (ожил hallucination_silence_threshold), дефолтный блэклист галлюцинаций в коде (в конфиге августа его не было — фильтр работал с пустым списком), фактическая модель в quality_report, warning про beam_size на MLX; замер на 20-мин срезе августа: подозрительных 64→46, «и и и» исчезли, субтитровые кредиты отбиваются (60 rejected), clean 876→921 — VAD вернул съеденную речь; 240 тестов зелёные; дальше — cross-track gating и полный перегон августа
- 2026-08-26 (3): cross-track gating — дорожки P8 синхронны сэмпл-в-сэмпл, окно 30 мс остаётся на дорожке только если она лидер по RMS или в 6 dB от лидера; убрано 39–62% таймлайна на дорожку, deduped_from 62→3, подозрительных 46→22; добавил exact-match блэклист против голых «Спасибо.» из приглушённых окон (172 rejected на срезе); 246 тестов; clean 20-мин среза 876→499 — плотность 1 реплика/2.4 с, похоже на правду вместо прежней каши; дальше — вычитка среза глазами и полный перегон августа
- 2026-08-26 (4): полный перегон августа новым кодом, ~3 часа конец-в-конец (транскрипция ~2.5 ч из-за word_timestamps, AI-batch на удивление быстро): raw 20176→12745, clean 15488→8746, deduped_from 1056→68 (−94%), подозрительных 708→455, rejected 906→2584 (фильтры ловят); PDF пересобран (Session_august_Report.pdf, 12 МБ, пока без сцен); вопрос на вычитку: у Мастера clean просел сильнее всех (4248→1842) — проверить на эталоне, не съедает ли гейт/фильтры его длинные монологи; дальше — bench init (эталонное окно) и вычитка
- 2026-08-26 (5): матокорпус по clean августа (505 вхождений, лидер Ангрон 300; заодно нашли 18 выживших межмикрофонных дублей — все в моменты ора, план: дедуп по пословным таймкодам, а не хардвар); спланировали выездную игру на Q2n-4K без поканалки — см. IDEAS «Выездная игра»

- 2026-08-31: переписал README под читателя с GitHub — сверху «что это / зачем / как работает / кому подойдёт / быстрый старт» с превью PDF (docs/images/pdf-preview.png, 4 страницы июньской хроники) и примерами реальных отчётов, весь прежний конфиг-справочник уехал под заголовок «Справочник» без потерь; дальше — решить, оставлять ли в репо превью с реальными именами партии
- 2026-08-31 (2): сверил справочную часть README с кодом — нашёл и починил: дефолтный backend в коде faster_whisper (README обещал mlx, mlx стоит только в config.example.json), пороги noise gate в README были из головы вместо реальных значений профилей (gentle -52/3, balanced -46/5, aggressive -40/8), не документированы --limit-minutes и ai.structured_output, в дереве выходных файлов не было preprocessed/ и work/, у run не сказано что без AI-этапа отчёты и PDF не собираются, у ai-analyze — что он и отчёты строит; попутно: hallucination_filter.mode в config.example.json мёртвый ключ, .gitignore перечисляет June/, а папка на диске june/ — на macOS git ловит по core.ignorecase, на Linux не поймает
